import * as api from '../Apis';

/* export const genereteToken = () => {
    return async (dispatch) => {
        const {request_token} = await api.genereteToken();
        localStorage.setItem('request_token', request_token);
        return;
    };
}; */